// Kept for backwards compatibility with any legacy imports
export const mockUsers = {};
export function getUser(username) { return null; }
export function createUser(username, displayName, password) { return null; }
export function addInfo(username, info) { return null; }
export function removeInfo(username, infoId) { return false; }
export function updateUser(username, updates) { return null; }
